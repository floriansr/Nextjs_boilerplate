language: node_js
node_js:
  - '15'
branches:
only:
  - master
  - develop
cache:
yarn: true
directories:
  - node_modules
before_install:
  - yarn upgrade
install:
  - yarn
script:
  - yarn test
  - yarn coveralls
